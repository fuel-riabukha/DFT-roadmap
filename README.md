# Digital Finance Team — Product Roadmap

Auto-generated interactive roadmap for the Fuel Digital Finance Team (DFT) product.

- **Source**: Google Sheet `Features in work (DFT)` (`1qyZK4YNsZqXwAAfpfNZj1Ajh8qk1YOvYRo7eEeUCvK4`)
- **Generator**: `generate_roadmap.py` (reads Sheets → rewrites `index.html`)
- **Deploy**: Netlify via GitHub Actions

## Regenerate
GitHub → **Actions → Regenerate from Google Sheets → Run workflow** (or push a change to `generate_roadmap.py`).

## Required GitHub Secrets
`SERVICE_ACCOUNT_JSON`, `NETLIFY_AUTH_TOKEN`, `NETLIFY_SITE_ID`

The Google Sheet must be shared (Editor) with the service-account email in `SERVICE_ACCOUNT_JSON`.
